services:
  kptv-proxy:
    image: ghcr.io/kpirnie/kptv-proxy:latest
    container_name: kptv_proxy
    restart: unless-stopped
    ports:
      - WHATEVER_PORT_YOU_WANT_TO_USE:8080
    volumes:
      - ./settings/:/settings/
      # to utilize ffmpeg, you must have it installed on your machine and add these 2 volumes
      # make sure to change the path to where ffmpeg and ffprobe are located on your machine
      #- /usr/local/bin/ffmpeg:/YOUR/PATH/TO/ffmpeg:ro
      #- /usr/local/bin/ffprobe:/YOUR/PATH/TO/ffprobe:ro

    # for hardware accelleration with ffpmeg
    #devices:
    #  - /dev/dri:/dev/dri

    # Health check
    healthcheck:
      test: [ "CMD", "curl", "-v", "http://127.0.0.1:8080/api/stats" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
